  
  ///app/app.php
  
  /// 1- for practice purpose CSRF token is disabled
  
  ->withMiddleware(function (Middleware $middleware) {

       
        $middleware->validateCsrfTokens(except: [
            '*',
        ]);

2- RequestController create and url mapping and blade template show

-http://127.0.0.1:8000/request

return view(view: 'request_practice');

3-query string
http://127.0.0.1:8000/request?name=Hafiz&email=info@gmail.com

-data catch or receive

$myQuery= $request->query();
       return $myQuery;

4- Parameter send and receive

-http://127.0.0.1:8000/prams/Hafizur/Pabna

-Route::get('/prams/{name}/{city}', [RequestController::class, 'prams']);
-prams receive

 $name= $request->name;

    $city= $request->city;

        return "My name is {$name} and My city {$city}";


5- Json Body//input and post method

http://127.0.0.1:8000/myJson

Route::post('myJson', [RequestController::class, 'myJson']);

function myJson(Request $request)
{
    $data= $request->input();
    return $data;
}

//data post by body
{
    "name":"Hafizur Rahman",
    "email":"admin@hafiz.com",
    "pass":"1234"
}

6- Laravel header Practice

7-form_data practice for file upload

8- Laravel Ip Address